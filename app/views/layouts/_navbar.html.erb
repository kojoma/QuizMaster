<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to 'QuizMaster', root_path, {:class => "navbar-brand"} %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav  mr-auto">
      <li class="nav-item<%= ' active' if controller.controller_name == 'quiz' %>">
        <%= link_to 'Quiz Mode', quiz_path, {:class => "nav-link"} %>
      </li>
      <li class="nav-item<%= ' active' if controller.controller_name == 'questions' %>">
        <%= link_to 'Manage Question', questions_path, {:class => "nav-link"} %>
      </li>
    </ul>
  </div>
</nav>
